# Sgen API Configuration (handles both planning and article generation)
BASE_URL=http://localhost:3001

# Website to Generate Content For
URL=https://example.com

# Content Generation Settings
TARGET=10           # Target number of articles in content plan
CLI_GEN_LIMIT=3     # Number of articles to generate

# Timeout Settings (milliseconds)
CLI_TIMEOUT_MS=60000           # Default API request timeout
