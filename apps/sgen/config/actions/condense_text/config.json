{
  "ai_routes": [
    { "model": "gpt-4o", "endpoint": "https://api.openai.com/v1", "api_key_env": "OPENAI_API_KEY" },
    { "model": "openai/gpt-4o", "endpoint": "https://openrouter.ai/api/v1", "api_key_env": "OPENROUTER_API_KEY" }
  ],
  "output_mode": "text_replace_all",
  "description": "Condense article text by removing verbosity, redundancy, and filler while preserving value"
}
