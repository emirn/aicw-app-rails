# aicw-app-rails Kamal Deployment Environment Variables
# Copy to .env.kamal and fill in values for local deployment
# For GitHub Actions, these are configured as repository secrets

# === Registry ===
KAMAL_REGISTRY_USERNAME=your-github-username
KAMAL_REGISTRY_PASSWORD=ghp_your_github_pat

# === Server ===
AICW_RAILS_SERVER_IP=your.server.ip
AICW_RAILS_DOMAIN=your-domain.com
AICW_RAILS_DEPLOY_USERNAME=ubuntu

# === Rails ===
RAILS_MASTER_KEY=your-rails-master-key

# === Google OAuth ===
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# === Tinybird (analytics) ===
TINYBIRD_API_KEY=your-tinybird-api-key

# === Website Builder ===
# WEBSITE_BUILDER_API_KEY is used by Rails to authenticate requests to website-builder.
# The website-builder service reads the same value as AICW_WEBSITE_BUILD_API_KEY.
# Both are set from this single secret — see .kamal/secrets for the mapping.
WEBSITE_BUILDER_API_KEY=your-shared-api-key

# === Cloudflare (for website-builder accessory) ===
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id
CLOUDFLARE_AICW_ZONE_ID=your-cloudflare-zone-id

# === Sgen (AI generation) ===
OPENROUTER_API_KEY=your-openrouter-api-key

# === Supabase (project sync for analytics validation) ===
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
